<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/weatherFragment">

    <fragment
        android:id="@+id/weatherFragment"
        android:name="com.rana.myweatherapp.presentation.weather.WeatherFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_weather" />

    <fragment
        android:id="@+id/DetailsFragment"
        android:name="com.rana.myweatherapp.presentation.details.DetailsFragment"
        android:label="@string/weather_details"
        tools:layout="@layout/fragment_details" />
    
    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.rana.myweatherapp.presentation.settings.SettingsFragment"
        android:label="@string/settings"
        tools:layout="@layout/fragment_settings" />
</navigation>